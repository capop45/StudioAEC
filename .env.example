# Copie para .env e edite. NÃO commitar o arquivo .env final.
#
# JWT — obrigatório em produção. Deve ter pelo menos 32 caracteres.
# Gere com: openssl rand -base64 48
JWT_KEY=mude-esta-chave-em-producao-minimo-32-caracteres-aqui!!
JWT_ISSUER=StudioAEC
JWT_AUDIENCE=StudioAEC

# Porta exposta do front (nginx) — padrão 80
WEB_PORT=80
